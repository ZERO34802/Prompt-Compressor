{"template_id": "T1", "cluster_size": 6, "template_text": "In {file} line {line_number}, {function}() raises {error_type} because {root_cause}; {fix_direction}.", "slots": ["file", "line_number", "function", "error_type", "root_cause", "fix_direction"], "example_prompts": ["In parser.py line 88, tokenize() drops the last element because the final unterminated segment is never flushed; flush the remaining buffer after the loop without changing the function's signature.", "In scripts/sync-items.js, the affected function raises FetchError because unbounded concurrency opens one socket per item at once; bound it with a worker pool and keep the existing behaviour for the passing cases.", "In tests/test_cart.py, cart() raises AssertionError because the module-scoped fixture is shared mutable state across tests; make the fixture function-scoped and keep the existing behaviour for the passing cases."], "avg_tokens_saved": 214.67, "avg_turns_saved": 4.33, "language": "python"}
{"template_id": "T2", "cluster_size": 4, "template_text": "In {file}, {function}() raises {error_type} because {root_cause}; {fix_direction}.", "slots": ["file", "function", "error_type", "root_cause", "fix_direction"], "example_prompts": ["In api/routes.js, the affected function returns a 500 instead of a proper error response because the rejected promise escapes the handler uncaught; catch it and map it to the right status code.", "In src/components/Dashboard.jsx, Dashboard() re-renders infinitely because a new object identity every render invalidates the effect dependency; depend on primitive values or memoise the object without changing the function's signature.", "In src/SignupForm.jsx, SignupForm() reloads the page instead of submitting because the native form submission is not prevented; handle onSubmit and call preventDefault without changing the function's signature."], "avg_tokens_saved": 262.0, "avg_turns_saved": 4.5, "language": "javascript"}
{"template_id": "T3", "cluster_size": 3, "template_text": "In {file}, {function}() misbehaves because {root_cause}; {fix_direction}.", "slots": ["file", "function", "root_cause", "fix_direction"], "example_prompts": ["In etl/clean.py, clean_orders() produces wrong output because dropna() with no subset drops rows with a null in any column; scope the drop with subset= without changing the function's signature.", "In src/controllers/orders.js, listOrders() returns an empty result because the response is sent before the async query resolves; await the query and return its rows without changing the function's signature.", "In reports/views.py, export_orders() times out because each loop iteration lazily triggers another query; batch the related fetches with select_related/prefetch_related without changing the function's signature."], "avg_tokens_saved": 246.0, "avg_turns_saved": 4.33, "language": "python"}
